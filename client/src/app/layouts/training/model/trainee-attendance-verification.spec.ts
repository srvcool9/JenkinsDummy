import { TraineeAttendanceVerification } from './trainee-attendance-verification';

describe('TraineeAttendanceVerification', () => {
  it('should create an instance', () => {
    expect(new TraineeAttendanceVerification()).toBeTruthy();
  });
});
