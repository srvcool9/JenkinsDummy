import { TrainerAttendanceVerification } from './trainer-attendance-verification';

describe('TrainerAttendanceVerification', () => {
  it('should create an instance', () => {
    expect(new TrainerAttendanceVerification()).toBeTruthy();
  });
});
