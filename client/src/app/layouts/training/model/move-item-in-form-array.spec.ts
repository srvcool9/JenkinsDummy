import { MoveItemInFormArray } from './move-item-in-form-array';

describe('MoveItemInFormArray', () => {
  it('should create an instance', () => {
    expect(new MoveItemInFormArray()).toBeTruthy();
  });
});
