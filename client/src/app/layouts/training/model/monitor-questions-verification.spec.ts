import { MonitorQuestionsVerification } from './monitor-questions-verification';

describe('MonitorQuestionsVerification', () => {
  it('should create an instance', () => {
    expect(new MonitorQuestionsVerification()).toBeTruthy();
  });
});
