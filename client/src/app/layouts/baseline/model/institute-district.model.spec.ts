import { InstituteDistrict } from './institute-district.model';

describe('InstituteDistrict', () => {
  it('should create an instance', () => {
    expect(new InstituteDistrict()).toBeTruthy();
  });
});
