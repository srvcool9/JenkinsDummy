import { LoggedinUserData } from './loggedin-user-data.model';

describe('LoggedinUserData', () => {
  it('should create an instance', () => {
    expect(new LoggedinUserData()).toBeTruthy();
  });
});
